<tr class="pin">
  <td>
    <%=link_to(image_tag(pin.image.thumb.url), pin) %>
    <p>
      <strong>Board:</strong>
      <%= pin.board.title %><br/>
      <%= pin.board.user.email %>
    </p>
    <p>
      <%= link_to 'Edit', edit_pin_path(pin) %> |
      <%= link_to 'Destroy', pin, method: :delete, data: {confirm: 'Are you sure?'} %>
    </p>
  </td>
</tr>